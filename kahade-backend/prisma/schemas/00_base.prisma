// Base enums and types used across all models

enum UserRole {
  USER
  ADMIN
  MODERATOR
}

enum UserStatus {
  ACTIVE
  INACTIVE
  SUSPENDED
  BANNED
}

enum TransactionStatus {
  PENDING
  PAYMENT_CONFIRMED
  COMPLETED
  DISPUTED
  CANCELLED
  REFUNDED
}

enum DisputeStatus {
  PENDING
  UNDER_REVIEW
  RESOLVED
  REJECTED
}

enum NotificationType {
  TRANSACTION
  DISPUTE
  PAYMENT
  SYSTEM
}
