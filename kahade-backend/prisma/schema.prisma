// ============================================================================
// KAHADE P2P ESCROW PLATFORM - MAIN DATABASE SCHEMA
// ============================================================================
// This is the main schema file that consolidates all split schemas
// using Prisma's schema folder feature for better organization.
//
// Split schema files located in: ./schema/
// ============================================================================

generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["prismaSchemaFolder"]
  binaryTargets   = ["native", "linux-musl-openssl-3.0.x", "debian-openssl-3.0.x"]
  // Support for Docker Alpine and Debian containers
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// ============================================================================
// SCHEMA IMPORTS
// ============================================================================
// All schema definitions are organized in separate files under ./schema/
// This approach provides:
// - Better code organization
// - Easier maintenance
// - Clear separation of concerns
// - Team collaboration friendly
//
// Schema organization:
// 00_base.prisma          - Base configuration and enums
// 10_user_auth.prisma     - User authentication and sessions
// 20_wallet_ledger.prisma - Wallet and financial ledger
// 25_admin_security.prisma - Admin and security features
// 30_payment.prisma       - Payment processing
// 40_bank_withdrawal.prisma - Bank accounts and withdrawals
// 50_order_escrow.prisma  - Orders and escrow
// 60_dispute.prisma       - Dispute resolution
// 70_rating.prisma        - Rating and reviews
// 80_kyc.prisma          - KYC verification
// 90_referral.prisma     - Referral system
// 100_promo_voucher.prisma - Promotions and vouchers
// 110_activity.prisma    - Activity logging
// 120_notification.prisma - Notifications
// 130_audit_compliance.prisma - Audit trails
// 140_system_config.prisma - System configuration
// 150_scheduled_jobs.prisma - Background jobs
//
// Note: With prismaSchemaFolder feature enabled, Prisma automatically
// discovers and includes all .prisma files in the schema/ directory.
// ============================================================================

// Schema files are automatically imported from ./schema/ directory
// No manual imports needed with prismaSchemaFolder feature