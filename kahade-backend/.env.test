# Test Environment Configuration

NODE_ENV=test
PORT=3001
API_PREFIX=api/v1

# Database (Test DB)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/kahade_test?schema=public

# JWT
JWT_SECRET=test-jwt-secret
JWT_EXPIRATION=15m
JWT_REFRESH_SECRET=test-refresh-secret
JWT_REFRESH_EXPIRATION=7d

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=1

# Blockchain (Mock)
BLOCKCHAIN_NETWORK=localhost
BLOCKCHAIN_RPC_URL=http://localhost:8545
SMART_CONTRACT_ADDRESS=0x0000000000000000000000000000000000000000
PRIVATE_KEY=test-private-key

# Payment Gateway (Mock)
PAYMENT_GATEWAY_API_KEY=test-api-key
PAYMENT_GATEWAY_SECRET=test-secret
PAYMENT_CALLBACK_URL=http://localhost:3001/api/v1/payments/callback
PAYMENT_ENVIRONMENT=test

# Email (Mock)
MAIL_HOST=localhost
MAIL_PORT=1025
MAIL_USER=test
MAIL_PASSWORD=test
MAIL_FROM=test@kahade.local

# File Upload
MAX_FILE_SIZE=1048576
UPLOAD_DEST=./test-uploads

# Security
RATE_LIMIT_TTL=60
RATE_LIMIT_LIMIT=1000
CORS_ORIGIN=*

# Logging
LOG_LEVEL=error
